# Career-Ops Pipeline Inbox

Add one job URL per line. Lines starting with `#` are comments (skipped).
Lines starting with `✓` are already processed (skipped).

Run `/career-ops pipeline` to process all pending URLs.

---

# Example (delete when you add real entries):
# https://company.com/jobs/senior-engineer
